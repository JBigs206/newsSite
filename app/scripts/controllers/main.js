'use strict';

/**
 * @ngdoc function
 * @name newsSiteApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the newsSiteApp
 */
angular.module('newsSiteApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
